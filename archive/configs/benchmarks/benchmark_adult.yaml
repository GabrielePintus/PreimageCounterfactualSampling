seed: 42

dataset:
  name: adult
  params:
    data_dir: data/

# preprocessing:
#   name: pca
#   params:
#     n_components: 0.99
#     svd_solver: full
#     whiten: false
#     random_state: 42


model:
  name: tabular_classifier_ckpt
  params:
    checkpoint: checkpoints/adult_classifier/best.ckpt
    device: cuda

sampling:
  n_queries: 200

timeout_per_sample: 60   # seconds per (method, query); set to 0 to disable

output:
  path: results/benchmark_adult_4.parquet

methods:

  # # --- Nearest Neighbor (no hyperparameters) ---
  # # --- Nearest Neighbor ---
  # # Full dataset baseline
  # - name: nearest_neighbor
  #   run_name: nn_full
  #   params: {}

  # # Grid: subsample_method=kmeans x k_per_class
  # - name: nearest_neighbor
  #   run_name: nn_kmeans
  #   params:
  #     subsample_method: kmeans
  #     k_per_class: [100, 200, 500, 1000]

  # # Grid: subsample_method=kmedoids x k_per_class
  # - name: nearest_neighbor
  #   run_name: nn_kmedoids
  #   params:
  #     subsample_method: kmedoids
  #     k_per_class: [100, 200, 500, 1000]

  # --- Growing Spheres ---
  # Grid: radius_step x n_in_layer (fix max_radius=5.0)
  # - name: growing_spheres
  #   run_name: gs
  #   params:
  #     max_radius: 5.0
  #     radius_step: [0.05, 0.15, 0.30]
  #     n_in_layer: [256, 512, 1024]

  # # --- DiCE ---
  # # Grid: proximity_weight x yloss_type (fix diversity_weight=0.0, total_cfs=1)
  # - name: dice
  #   run_name: dice
  #   params:
  #     total_cfs: 1
  #     diversity_weight: 0.0
  #     proximity_weight: [0.5, 1.0, 2.0]
  #     yloss_type: [hinge_loss, log_loss]

  # --- FACE ---
  # - name: face
  #   run_name: face_knn
  #   params:
  #     density_estimator:
  #       name: kde
  #     epsilon: 2.0
  #     subsample_method: kmedoids
  #     k_per_class: 1000

  # # --- Nearest Neighbor ---
  # - name: nearest_neighbor
  #   run_name: nn_full
  #   params:
  #     norm: 2
  #     # subsample_method: kmedoids
  #     # k_per_class: 1000

  # # Grid: proximity_weight x yloss_type (fix diversity_weight=0.0, total_cfs=1)
  # - name: dice
  #   run_name: dice
  #   params:
  #     total_cfs: 1
  #     diversity_weight: 0.0
  #     min_iter: 50

  # --- CertCF ---
  - name: certcf
    run_name: certcf_smoke
    params:
      checkpoint: checkpoints/adult_classifier/best.ckpt
      device: cuda
      norm: 1
      eps_alpha: [0.15, 0.25, 0.35, 0.45]
      batch_size: 256
      subsample_method: kmedoids
      k_per_class: [50, 100, 200]
